#include <stdio.h>
/**
 * main -  program that prints all single digit no of base 10 starting from 0
 *
 * Return:- always 0
 *
 */
int main(void)
{
	int n = 0;

	for (n < 10 ; n++;)
		printf("%d", n);
	extern int putchar (int __c);
		return (0);
}

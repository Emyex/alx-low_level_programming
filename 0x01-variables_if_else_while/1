#include <stdio.h>
/**
 * main -  program that prints all single digit no of base 10 starting from 0
 *
 * Return:- always 0
 *
 */
int main(void)
{
	int no = 0;

	for (no < 10 ; no++)
		printf("%d", no);
	int ("\n");
		return (0);
}
